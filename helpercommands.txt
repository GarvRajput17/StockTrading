//COMPILE JAVA WITH JSON EXECUTABLE
javac -cp .:lib/json-20240303.jar AlphaVantageAPI.java 

//RUN JAVA FILE DIRECTLY
java -cp .:lib/json-20240303.jar AlphaVantageAPI.java <stock-symbol>

--- DOCKER COMMANDS ---

//BUILD THE DOCKER IMAGE
docker build -t stock-app .

//RUN DOCKER CONTAINER
docker run -it stock-app

//compile cpp with curl
g++ main1.cpp -lcurl -o program

// compile cpp with JNI
g++ main1.cpp -ljvm

// compile cpp with openSSL
g++ main1.cpp -lssl -lcrypto -lcurl -o program
g++ main1.cpp -I/opt/homebrew/opt/openssl/include -L/opt/homebrew/opt/openssl/lib -lssl -lcrypto -lcurl -o program
